name: draft-spec
description: "Draft the initial type specification for the '@{{ directiveName }}' directive based on synthesized requirements."
# No for_each, this is a single architect task

tasks:
  - useTask: draft-initial-directive-spec
    prior_outputs:
      # Map the output from the final consolidation task in the previous set
      synthesized_requirements: "{{ synthesize-requirements.consolidate-synthesized-reqs.output }}"
    # Task context will include:
    # - synthesized_requirements (the consolidated requirements doc)
    # - global_variables (overallArchitecture, directiveName, directiveClarityContent) 