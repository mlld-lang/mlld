---
id: output
title: Output
brief: Writing data to files, stderr, and streams
category: output
updated: 2026-02-25
---

`output` writes to files and stdout. `log` writes to stderr. `append` adds to existing files. `stream` enables real-time token-by-token output from LLM calls.
