---
id: commands
title: Commands
brief: Execute code, define functions, and manage output
category: commands
updated: 2026-01-05
---

`run` executes commands and code. `exe` defines reusable functions. `show` writes document output, `log` writes stderr, and `=>` returns explicit script output. `hook` registers lifecycle hooks for observability and transforms.
