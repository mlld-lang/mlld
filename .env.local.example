# Local Environment Configuration
# Copy this file to .env.local and customize for your development environment
# .env.local is gitignored and won't be committed

# Fast Test Mode
# Set to 'true' to skip slow edge case tests during development
# CI will always run the full test suite regardless of this setting
TESTFAST=false

# To enable fast tests:
# TESTFAST=true

# You can also override this per-run:
# TESTFAST=true npm test   (run fast)
# TESTFAST=false npm test  (run full suite)
