## Output Format

Return valid JSON. No markdown fences, no commentary outside the JSON object. Every field in the schema must be present.

Include a `status` field: "completed" if you finished the task, "partial" if work remains, "failed" if you could not proceed.
