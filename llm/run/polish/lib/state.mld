>> State Management Library (legacy — checkpoint system handles run state)
>> Kept for @pipelineRoot variable used by other modules.

>> Pipeline root directory
var @pipelineRoot = "@base/pipeline"

/export {
  @pipelineRoot
}
