# Chaining LLM Requests Example
@import [role] from [imports.mld]

@text tests = @run [npm test]
@text res = @run [oneshot "What's broken here? {{tests}}"]
@run [oneshot "Make a plan to fix {{res}} --system {{role.architect}}"]