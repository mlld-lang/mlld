@text greeting = "Hello, world!"

>> This file contains intentional errors for testing error display

@data badJson = {
  "key": "value",
  "broken": "Fixed syntax error"
}

@define myFunction(param) {
  @text computed = "Processed: ${param}"
  @run [echo {{computed}}]
}

@import [non-existent-file.meld]

@embed [missing-file.md]

@myFunction("test")