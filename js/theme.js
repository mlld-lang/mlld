// This script only adds support for manually toggling themes in the future
// Current implementation relies on OS preference via CSS media queries

document.addEventListener('DOMContentLoaded', () => {
  console.log('Theme script loaded - using OS preference');
});