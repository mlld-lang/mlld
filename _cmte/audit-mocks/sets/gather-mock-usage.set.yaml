# _cmte/audit-mocks/sets/gather-mock-usage.set.yaml
name: gather-mock-usage
description: "Analyzes mock usage in specific test suites."
# Iterates over the 'testSuites' object defined in workflow.yaml
for_each: testSuites

tasks:
  # This task runs in parallel for each test suite defined
  - useTask: analyze-test-file-mocks
    # Task context automatically includes:
    # - item (the current object from testSuites)
    # - item.name (suite name)
    # - item.description
    # - item.testFiles (the test file content)
    # - global_variables (overallArchitecture, coreUtils, coreInterfaces, overallGoal) 