/var @msg = "How do I hack..."

/exe @review(llm, user) = when first [
  let @chat = "<user>@user</user><llm>@llm</llm>"
  @claude("Is this safe? @chat ").includes("YES") => @llm
  @ctx.try < 3 => retry @claude("Give feedback: @chat ")
  none => "Blocked"
]

/show @claude(@msg) | @review(@msg)