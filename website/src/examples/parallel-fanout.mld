exe @review(f) = [
  let @result = @f | cmd { claude -p "Review this:" }
  => { file: @f.mx.relative, review: @result.trim() }
]

var @files = <src/**/*.ts>
var @reviews = for parallel(5) @f in @files => @review(@f)