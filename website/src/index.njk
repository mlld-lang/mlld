---
layout: base.njk
---

<section class="hero">
  <div class="hero-content">
    <h1 class="hero-logo">mlld</h1>
    <p class="tagline">modular prompt scripting</p>
    <div><code>npm install -g meld oneshot</code></div>
    <p>Get the vscode/cursor extension</p>
  </div>
  
  <div class="hero-terminal-container">
    <div class="hero-terminal">
      <div class="terminal-dots">
        <span class="dot red"></span>
        <span class="dot yellow"></span>
        <span class="dot green"></span>
      </div>

      <div id="terminal-modules" class="terminal-content active"><span class="token keyword">@import</span> <span class="token bracket">[</span><span class="token variable">roles</span><span class="token bracket">, </span><span class="token variable">tasks</span><span class="token bracket">]</span> <span class="token keyword">from</span> <span class="token bracket">[</span>prompts.mld<span class="token bracket">]</span>
<span class="space"></span>
<span class="token keyword">@text</span> <span class="token variable">arch</span> = <span class="token keyword">@embed</span> <span class="token bracket">[</span>README.md <span class="token selector"># Architecture</span><span class="token bracket">]</span>
<span class="token keyword">@text</span> <span class="token variable">standards</span> = <span class="token keyword">@embed</span> <span class="token bracket">[</span>README.md <span class="token selector"># Code Standards</span><span class="token bracket">]</span>
<span class="token keyword">@text</span> <span class="token variable">diff</span> = <span class="token keyword">@run</span> <span class="token bracket">[</span>git diff | cat<span class="token bracket">]</span>
<span class="space"></span>
<span class="token keyword">@text</span> <span class="token variable">prompt</span> = <span class="token bracket">[[</span>
   Read our docs: <span class="token variable">{% raw %}{{arch}}{% endraw %}</span> <span class="token variable">{% raw %}{{standards}}{% endraw %}</span>
   Latest changes: <span class="token variable">{% raw %}{{diff}}{% endraw %}</span>
   Your task: <span class="token variable">{% raw %}{{tasks.codereview}}{% endraw %}</span>
<span class="token bracket">]]</span>
<span class="space"></span>
<span class="token keyword">@run</span> <span class="token bracket">[</span>oneshot <span class="token variable">{% raw %}{{prompt}}{% endraw %}</span> --system <span class="token variable">{% raw %}{{roles.architect}}{% endraw %}</span><span class="token bracket">]</span>
<span class="space"></span>
<span class="token operator">---</span>
<span class="animated-line" data-text="Looking at the TypeBlorp updates, the new unidirectional flow implementation correctly separates concerns between state mutations and subscriptions. The observer pattern implementation is clean but has potential race conditions in the notification queue that should be addressed with a proper message ordering mechanism. I recommend adding tests for concurrent updates."></span>
      </div>

      <div id="terminal-scripts" class="terminal-content" style="display: none;"><span class="token bracket">#</span> <span class="token selector">TypeBlorp</span>

<span class="token bracket">##</span> <span class="token selector">Overview</span> 
TypeBlorp is lightweight state management library built with TypeScript, with a unidirectional data flow pattern combined with observer-based pub/sub architecture.

Here's the structure of the codebase:
<span class="token keyword">@run</span> <span class="token bracket">[</span>tree --gitignore<span class="token bracket">]</span>

<span class="token keyword">@embed</span> <span class="token variable">{% raw %}{{docs.architecture}}{% endraw %}</span> <span class="token keyword">as</span> <span class="token bracket">##</span> <span class="token selector">Architecture</span>
<span class="token keyword">@embed</span> <span class="token variable">{% raw %}{{docs.standards}}{% endraw %}</span> <span class="token keyword">as</span> <span class="token bracket">##</span> <span class="token selector">Code Standards</span>

<span class="animated-line" data-text="Based on the failing tests, we should add test cases for concurrent state updates, race condition handling, and the new observer pattern. I recommend focusing on the notification queue implementation first, adding tests for proper message ordering and ensuring subscribers receive updates in the expected sequence."></span>
      </div>
    </div>
    <div class="terminal-toggle">
      <p>Treat markdown as</p>
      <div class="toggle-buttons">
        <button id="toggle-modules" class="toggle-btn active">modules</button>
        <button id="toggle-scripts" class="toggle-btn">scripts</button>
      </div>
    </div>
  </div>
</section>

<section>  
  <h2>What is prompt scripting?</h2>
  <p>If you've already experienced the <strong>pain</strong>, you already know what you <strong>want</strong> it to be.</p>
  <div class="examples">
    <div>
      <h3>Tired of repeating yourself</h3>
      <p>"Working with LLMs is like having a brilliant intern with chronic amnesia."</p>
    </div>
    <div>
      <h3>Tired of curating context</h3>
      <p>"I'd do a lot more with LLMs if re-assembling context wasn't such a chore."</p>
    </div>
    <div>
      <h3>Tired of wrong tools for the job</h3>
      <p>"I just want to script LLMs. A chat or an IDE isn't helpful for that."</p>
    </div>
  </div>
</section>

<section>  
  <h2>What is prompt scripting?</h2>
  <p>If you've already seen the <strong>possibility</strong>, you already know what you <strong>want</strong> it to be.</p>
  <div class="examples">
    <div>
      <h3>Write scripts to embed LLM context in the middle of your markdown docs.</h3>
      <pre>
<code>
  # Overview

  Here's the structure of the codebase:
  
  <span class="token keyword">@run</span> <span class="token bracket">[</span>tree --gitignore<span class="token bracket">]</span>  
</code>
      </pre>
    </div>
    <div>
      <h3>Chain LLM requests and responses like you'd pipe shell commands</h3>
      <pre>
<code>
<span class="token keyword">@text</span> <span class="token variable">tests</span> = <span class="token keyword">@run</span> <span class="token bracket">[</span>npm test<span class="token bracket">]</span>

<span class="token keyword">@text</span> <span class="token variable">res</span> = <span class="token keyword">@run</span> <span class="token bracket">[</span>oneshot "What's broken here? <span class="token variable">{% raw %}{{tests}}{% endraw %}</span>"<span class="token bracket">]</span>

<span class="token keyword">@run</span> <span class="token bracket">[</span>oneshot "Make a plan to fix <span class="token variable">{% raw %}{{res}}{% endraw %}</span>"<span class="token bracket">]</span>

<span class="token operator">---</span>

<span class="token string">"Here's a plan to fix the failing tests."</span>
</code>
    </pre>
    </div>
    <div>
      <h3>Use imports and treat markdown doc sections like modules</h3>
      <pre>
<code>
  <span class="token keyword">@import</span> <span class="token bracket">[</span><span class="token variable">roles</span><span class="token bracket">]</span> from <span class="token bracket">[</span>roles.mld<span class="token bracket">]</span>


  <span class="token keyword">@text</span> <span class="token variable">infra</span> = <span class="token keyword">@embed</span> <span class="token bracket">[</span>TESTING.md <span class="token selector"># Infrastructure</span><span class="token bracket">]</span>
  
  <span class="token keyword">@text</span> <span class="token variable">prompt</span> = <span class="token bracket">[[</span>
    Read our docs: <span class="token variable">{% raw %}{{infra}}{% endraw %}</span> <span class="token variable">{% raw %}{{standards}}{% endraw %}</span>
    Review this set of changes: <span class="token variable">{% raw %}{{diff}}{% endraw %}</span>
    Please create a list of the tests we
    should add to cover the changes.
  <span class="token bracket">]]</span>
  
  <span class="token keyword">@run</span> <span class="token bracket">[</span>oneshot <span class="token variable">{% raw %}{{prompt}}{% endraw %}</span> --system <span class="token variable">{% raw %}{{roles.architect}}{% endraw %}</span><span class="token bracket">]</span>

  <span class="token operator">---</span>

  <span class="token string">"Here's a list of the tests to add…"</span>  
</code>
      </pre>
    </div>
  </div>
</section>


  <div class="cta-link">
    <a href="/docs/introduction/">Read the Introduction →</a>
  </div>
</section>