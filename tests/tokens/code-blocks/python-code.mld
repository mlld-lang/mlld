/var @data = python {
  import json
  return json.dumps({"hello": "world"})
}

/exe @process(items) = python {
  filtered = [x for x in items if x > 0]
  return sum(filtered) / len(filtered) if filtered else 0
}

=== START TOKENS ===
@data --> variable[declaration]
= --> operator
python --> label
{ --> operator
  import json
  return json.dumps({"hello": "world"}) --> string
} --> operator
@process --> variable[declaration]
( --> operator
items --> parameter
) --> operator
= --> operator
python --> label
{ --> operator
  filtered = [x for x in items if x > 0]
  return sum(filtered) / len(filtered) if filtered else 0 --> string
} --> operator
=== END TOKENS ===