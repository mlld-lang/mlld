needs {
  cmd: [git, curl],
  node: [lodash]
}

var @policyConfig = {
  defaults: {
    rules: [
      "no-secret-exfil",
      "no-untrusted-destructive"
    ]
  },
  capabilities: {
    allow: ["cmd:git:*", "cmd:curl:*"],
    deny: ["sh"]
  }
}
policy @p = union(@policyConfig)