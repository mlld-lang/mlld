>> From if blocks
if @checkFailed [
  bail "validation failed"
]

>> From when expressions
when [
  !@ready => bail "not ready"
  * => @process()
]

>> From for loops
for @item in @items [
  if !@item.valid [
    bail `Invalid item: @item.id`
  ]
]