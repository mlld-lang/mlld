exe @source() = when [
  @mx.try == 1 => "draft"
  * => "final"
]

exe @validator() = when [
  @mx.input == "draft" => retry "missing title"
  * => `Used hint: @mx.hint`
]

var @result = @source() | @validator
show @result