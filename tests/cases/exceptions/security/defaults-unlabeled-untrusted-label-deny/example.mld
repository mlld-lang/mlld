var @policyConfig = {
  defaults: { unlabeled: "untrusted" },
  labels: {
    untrusted: { deny: ["op:cmd"] }
  },
  operations: {
    "exfil": ["op:cmd"]
  }
}
policy @p = union(@policyConfig)

var @data = <./defaults-unlabeled-label-deny-data.txt>
exe @echo(input) = run cmd { echo "@input" }
show @echo(@data)
