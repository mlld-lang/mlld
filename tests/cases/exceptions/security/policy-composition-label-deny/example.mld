var @base = {
  capabilities: {
    allow: ["cmd:echo:*"]
  }
}
policy @p1 = union(@base)

var @labelRules = {
  defaults: { rules: ["no-secret-exfil"] },
  operations: {
    "exfil": ["op:cmd"]
  }
}
policy @p2 = union(@labelRules)

var secret @token = "sk-123"
run cmd { echo "@token" }
