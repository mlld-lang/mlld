# Test run cmd syntax in var and exe contexts

Both `run {...}` and `run cmd {...}` should work consistently.

## In /var assignments

## In /exe definitions

## With stdin patterns

## Show results

implicit cmd
explicit cmd
implicit in exe
explicit in exe
test
test
