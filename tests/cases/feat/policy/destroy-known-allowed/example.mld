var @policyConfig = {
  defaults: { rules: ["no-destroy-unknown"] }
}
policy @p = union(@policyConfig)

exe @approve(fileId) = [
  => known @fileId
]

var @fileId = @approve("13")
exe destructive:targeted @deleteFile(fileId) = `delete:@fileId`

show @deleteFile(@fileId)
