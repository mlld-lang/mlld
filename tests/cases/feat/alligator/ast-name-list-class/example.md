# AST Name List - Classes

Test that `{ class?? }` returns only class names.

## List class names with class??

/var @classNames = <ast-name-list-class-service.ts { class?? }>

/show @classNames.join(", ")
