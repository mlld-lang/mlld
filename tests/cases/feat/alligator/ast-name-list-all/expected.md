# AST Name List - All Definitions

Test that `{ ?? }` returns a list of all definition names as strings.

## List all definition names with ??

IUser, Status, UserId, UserService, VERSION, createUser
